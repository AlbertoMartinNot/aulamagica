/*Este test comprueba si el botón de Registro se activa o no 
*dependiendo de la informacion que se introduce en el formulario,
*Con esta información sería correcto y se podría utilizar el boton Enviar.
*/
var webdriver = require('selenium-webdriver'),
By = webdriver.By,
until = webdriver.until;

var driver = new webdriver.Builder()
.forBrowser('chrome')
.build();

driver.get('http://localhost:8080/mayia.html#!/');
driver.findElement(webdriver.By.id('regi')).click()
//Rellena el formulario con los datos correctos
.then(
	driver.findElement(By.name('dni')).sendKeys('05449460R'),
	driver.findElement(By.name('usu')).sendKeys('berto'),
	driver.findElement(By.name('psw')).sendKeys('b3rt1122'),
	driver.findElement(By.name('mail')).sendKeys('albertomartinnotgmail.com'),
	driver.sleep(2000))
//Comprueba si el botón de Registro está habilitado o no
.then(function(data){
	if (driver.findElement(By.name('enviar')).isEnabled()) {
		console.log('Correcto');
	}else{
		console.log('Incorrecto');
	}
});




